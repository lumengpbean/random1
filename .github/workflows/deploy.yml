# Disabled — deployment now handled by Vercel
# Vercel auto-deploys when code is pushed to main
# The Next.js app requires server-side features (API routes, middleware)
# which are not supported by static GitHub Pages export

name: Deploy (disabled)
on: workflow_dispatch
jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deployment is now handled by Vercel"
